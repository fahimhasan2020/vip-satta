import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default function Hr() {
    return (
        <View style={{ width: '100%', padding: 1, backgroundColor: '#ccc', marginBottom: 20 }}>
        </View>
    )
}

const styles = StyleSheet.create({})